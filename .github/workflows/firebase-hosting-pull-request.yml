# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
'on': pull_request
env:
  NEXT_PUBLIC_FIREBASE_API_KEY: ${{ secrets.NEXT_PUBLIC_FIREBASE_API_KEY }}
  FIREBASE_SERVICE_ACCOUNT_DOC_GENIE_6B615: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_DOC_GENIE_6B615 }}
  FIREBASE_CLI_EXPERIMENTS: webframeworks
  MONGODB_URI: ${{ secrets.MONGODB_URI }}
  OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
  CUSTOM_FIREBASE_ADMIN_PROJECT_ID: ${{ secrets.CUSTOM_FIREBASE_ADMIN_PROJECT_ID }}
  CUSTOM_FIREBASE_ADMIN_CLIENT_EMAIL: ${{ secrets.CUSTOM_FIREBASE_ADMIN_CLIENT_EMAIL }}
  CUSTOM_FIREBASE_ADMIN_PRIVATE_KEY: ${{ secrets.CUSTOM_FIREBASE_ADMIN_PRIVATE_KEY }}
jobs:
  build_and_preview:
    if: '${{ github.event.pull_request.head.repo.full_name == github.repository }}'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_DOC_GENIE_6B615 }}'
          projectId: doc-genie-6b615
